# 话题
话题通信机制订阅发布的方式来传递消息，eg MQTT  （观察者模式）


如果你在做机器人，需要去ROS，需要找一个高效的消息中间件，我想ZMQ应该比较适合你，当然也可以根据机器人的处理器和性能试试ROS2的FastDDS
zmq也是一个订阅发布的方式来传递消息

话题机制有4个关键点 ：
    发布者， 订阅者， 话题名称， 话题类型（消息接口 ： 相当于话题的内容类型， 用来解读数据的    ）


## 订阅发布模型
一个节点接受的形式外（1对1），ROS2话题通信其实还可以是1对n,n对1,n对n的。

## 消息接口
为了方便发送者和接收者进行数据的交换，ROS2帮我们在数据传递时做好了消息的序列化和反序列化（有关消息序列化相关内容请参考本章基础篇），而且ROS2的消息序列化与反序列化通信是可以做到跨编程语言、跨平台和跨设备之间的。

ROS2如何做到跨编程语言、跨平台和跨设备之间的数据收发呢？这就得益于通过定义消息接口文件了
通过对消息接口介绍，相信你肯定能猜到这样一条规则：同一个话题，所有的发布者和接收者必须使用相同消息接口。

### 接口格式是啥样的 
    自定义的 

## gui工具 
rqt_graph

## CLI工具
为什么能查到
ros2 topic -h
ros2 topic list
ros2 topic list -t
ros2 topic echo /chatter
ros2 topic info  /chatter
ros2 interface show 查看消息类型
ros2 interface show std_msgs/msg/String
ros2 topic pub /chatter std_msgs/msg/String 'data: "123"'

## 话题通信机制使用



## 服务通信机制使用


## 自定义接口

## DDS原生介绍



# 服务 

# 动作

# 参数